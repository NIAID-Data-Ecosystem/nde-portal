import { TreeNode } from './helpers';

const defaultMargin = { top: 30, left: 30, right: 30, bottom: 70 };

export const TopicBrowser = ({ data }: { data: TreeNode }) => {
  return <div>TopicBrowser</div>;
};
